---
phase: 02-walk-around-the-warehouse
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Assets/_Project/Scripts/UI/VirtualJoystick.cs
  - Assets/_Project/Scripts/UI/WM.UI.asmdef
  - Assets/_Project/Scripts/Editor/Phase02JoystickBuilder.cs
  - Assets/_Project/Scenes/Warehouse_MVP.unity
  - Assets/Tests/EditMode/VirtualJoystickMathTests.cs
  - Assets/Tests/EditMode/WM.Tests.EditMode.asmdef
  - Assets/Tests/PlayMode/WM.Tests.PlayMode.asmdef
autonomous: true
requirements: [MOVE-01]
tags: [unity, ugui, virtual-joystick, input-system, editor-tooling, edit-mode-tests]

must_haves:
  truths:
    - "A bottom-left joystick widget is visible from scene load on Warehouse_MVP"
    - "Touching the joystick background and dragging produces a Vector2 with magnitude in [0,1]"
    - "Releasing the touch resets joystick.Direction to Vector2.zero and snaps the handle to center"
    - "Dead-zone math (raw < 0.1 -> zero; raw > 1 -> clamped to unit circle) is deterministic and unit-tested"
  artifacts:
    - path: "Assets/_Project/Scripts/UI/VirtualJoystick.cs"
      provides: "UGUI joystick MonoBehaviour with IPointerDown/IDrag/IPointerUp handlers, public Vector2 Direction, public static ApplyDeadZoneAndClamp, internal SetDirectionForTesting"
      min_lines: 70
      contains: "public Vector2 Direction"
    - path: "Assets/_Project/Scripts/Editor/Phase02JoystickBuilder.cs"
      provides: "Idempotent Editor builder that creates UICanvas/SafeAreaPanel/Joystick + Handle children with VirtualJoystick wired"
      min_lines: 80
      contains: "Tools/Phase02/Build Joystick"
    - path: "Assets/Tests/EditMode/VirtualJoystickMathTests.cs"
      provides: "Pure-function EditMode coverage for ApplyDeadZoneAndClamp"
      min_lines: 30
      contains: "ApplyDeadZoneAndClamp"
    - path: "Assets/_Project/Scenes/Warehouse_MVP.unity"
      provides: "Joystick GameObject under UICanvas/SafeAreaPanel after Phase02JoystickBuilder runs"
      contains: "VirtualJoystick"
  key_links:
    - from: "Assets/_Project/Scripts/UI/VirtualJoystick.cs"
      to: "UICanvas/SafeAreaPanel"
      via: "Editor builder parents joystick GameObject under SafeAreaPanel"
      pattern: "SafeAreaPanel"
    - from: "Assets/_Project/Scripts/UI/VirtualJoystick.cs"
      to: "RectTransformUtility.ScreenPointToLocalPointInRectangle"
      via: "OnPointerDown/OnDrag call ScreenPointToLocalPointInRectangle with eventData.pressEventCamera"
      pattern: "ScreenPointToLocalPointInRectangle"
    - from: "Assets/_Project/Scripts/Editor/Phase02JoystickBuilder.cs"
      to: "Warehouse_MVP.unity"
      via: "EditorSceneManager.OpenScene + SaveScene"
      pattern: "EditorSceneManager"
---

<objective>
Implement the on-screen virtual joystick widget: a UGUI MonoBehaviour anchored bottom-left under UICanvas/SafeAreaPanel that translates touch/drag into a normalized `Vector2 Direction`. This plan delivers the first user-observable behavior of Phase 02 — pressing Play (or running the iOS build) shows the joystick on screen and dragging the handle reports a `Direction` that downstream `PlayerController` (Plan 02-02) will consume.

Purpose: MOVE-01 acceptance starts with "Touching and dragging the on-screen joystick" — this plan owns the joystick widget end-to-end (script + scene authoring + Wave 0 math tests). The joystick is functionally testable in isolation through `SetDirectionForTesting` and `ApplyDeadZoneAndClamp` before the player controller exists.

Output:
- `Assets/_Project/Scripts/UI/VirtualJoystick.cs` (runtime UGUI component in `WM.UI`).
- `Assets/_Project/Scripts/Editor/Phase02JoystickBuilder.cs` (idempotent Editor builder that authors the joystick GameObject under `UICanvas/SafeAreaPanel`).
- Mutated `Warehouse_MVP.unity` containing the `Joystick` GameObject after the builder runs.
- `Assets/Tests/EditMode/VirtualJoystickMathTests.cs` (Wave 0 pure-function tests).
- Asmdef updates: `WM.Tests.EditMode` already references `WM.UI`; `WM.Tests.PlayMode` adds `WM.UI`, `WM.Player`, `Unity.Cinemachine` (PlayMode references staged here so Plan 02-02's PlayMode test files compile on first try).
</objective>

<execution_context>
@/Users/brunocerecetto/repos/mine/warehouse-maters/.claude/get-shit-done/workflows/execute-plan.md
@/Users/brunocerecetto/repos/mine/warehouse-maters/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-walk-around-the-warehouse/02-CONTEXT.md
@.planning/phases/02-walk-around-the-warehouse/02-RESEARCH.md
@.planning/phases/02-walk-around-the-warehouse/02-PATTERNS.md
@.planning/phases/02-walk-around-the-warehouse/02-VALIDATION.md
@.planning/phases/01-project-bootstrap-empty-warehouse-scene/01-03-SUMMARY.md
@specs/02-gameplay-systems-spec.md
@specs/07-mvp-backlog-acceptance-criteria.md
@Assets/_Project/Scripts/UI/SafeAreaPanel.cs
@Assets/_Project/Scripts/Editor/Phase01CameraUiBuilder.cs
@Assets/_Project/Scripts/UI/WM.UI.asmdef
@Assets/Tests/EditMode/BootstrapStructureTests.cs

<interfaces>
<!-- Key types and contracts. Extract from PATTERNS.md §1 and §6, RESEARCH.md §Pattern 1. -->

VirtualJoystick contract (this plan creates it):
```csharp
namespace WM.UI
{
    [RequireComponent(typeof(RectTransform))]
    public sealed class VirtualJoystick : MonoBehaviour,
        IPointerDownHandler, IDragHandler, IPointerUpHandler
    {
        [SerializeField] private RectTransform background;
        [SerializeField] private RectTransform handle;
        [SerializeField, Range(0f, 0.5f)] private float deadZone = 0.1f;

        public Vector2 Direction { get; private set; }
        public static Vector2 ApplyDeadZoneAndClamp(Vector2 raw, float deadZone);

        // Test seam — exposed to WM.Tests.PlayMode via InternalsVisibleTo.
        internal void SetDirectionForTesting(Vector2 dir);
    }
}
```

WM.UI.asmdef state (verified on disk, do not break):
```json
{
    "name": "WM.UI",
    "references": ["WM.Core", "WM.Economy", "WM.Orders", "WM.Upgrades"]
}
```
NOTE: `WM.UI` does NOT reference `WM.Player`; the back-edge `WM.Player -> WM.UI` is the only directional edge added by Phase 02 (Plan 02-02). This plan touches `WM.UI` only.

SafeAreaPanel parent (Phase 01 substrate, verified at `Assets/_Project/Scripts/UI/SafeAreaPanel.cs`):
```csharp
namespace WM.UI
{
    [RequireComponent(typeof(RectTransform))]
    public sealed class SafeAreaPanel : MonoBehaviour { /* runtime-applies Screen.safeArea */ }
}
```
The joystick goes under `UICanvas/SafeAreaPanel` so notch/Dynamic Island insets are honored.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Implement VirtualJoystick MonoBehaviour + Wave 0 math tests + asmdef edits</name>
  <read_first>
    - Assets/_Project/Scripts/UI/SafeAreaPanel.cs (sealed-Mono + RequireComponent template, file-header citation convention)
    - Assets/_Project/Scripts/UI/WM.UI.asmdef (verify references list before editing — must keep WM.Core/WM.Economy/WM.Orders/WM.Upgrades)
    - Assets/Tests/EditMode/WM.Tests.EditMode.asmdef (already references WM.UI per Plan 01-03 SUMMARY — verify)
    - Assets/Tests/PlayMode/WM.Tests.PlayMode.asmdef (currently only references WM.Core + UnityEngine.TestRunner — will need WM.UI + WM.Player + Unity.Cinemachine for Plan 02-02 PlayMode tests)
    - Assets/Tests/EditMode/BootstrapStructureTests.cs (NUnit test header + Assert.That style)
    - .planning/phases/02-walk-around-the-warehouse/02-RESEARCH.md §Pattern 1 (lines 166-243) and §Code Examples lines 666-710
    - .planning/phases/02-walk-around-the-warehouse/02-PATTERNS.md §1 and §9
  </read_first>
  <files>
    Assets/_Project/Scripts/UI/VirtualJoystick.cs (NEW)
    Assets/_Project/Scripts/UI/WM.UI.asmdef (MODIFIED — add `"allowUnsafeCode": false` only if missing; primary edit is to add InternalsVisibleTo via AssemblyInfo, see action)
    Assets/_Project/Scripts/UI/AssemblyInfo.cs (NEW — single-line `[assembly: InternalsVisibleTo("WM.Tests.PlayMode")]`)
    Assets/Tests/EditMode/VirtualJoystickMathTests.cs (NEW)
    Assets/Tests/EditMode/WM.Tests.EditMode.asmdef (MODIFIED — verify `"WM.UI"` already present; no edit expected per Plan 01-03 SUMMARY but VERIFY)
    Assets/Tests/PlayMode/WM.Tests.PlayMode.asmdef (MODIFIED — add `"WM.Player"`, `"WM.UI"`, `"Unity.Cinemachine"` to references; staged here so Plan 02-02 PlayMode tests compile on first run)
  </files>
  <action>
    1. Create `Assets/_Project/Scripts/UI/VirtualJoystick.cs` exactly per RESEARCH.md §Pattern 1 (lines 173-236) with these concrete additions:
       - File header citing `Plan 02-01 + D-01/D-02/D-03/D-04 + RESEARCH §Pattern 1`.
       - `namespace WM.UI`, `[RequireComponent(typeof(RectTransform))]`, `public sealed class VirtualJoystick : MonoBehaviour, IPointerDownHandler, IDragHandler, IPointerUpHandler`.
       - Serialized fields exactly: `[SerializeField] private RectTransform background;`, `[SerializeField] private RectTransform handle;`, `[SerializeField, Range(0f, 0.5f)] private float deadZone = 0.1f;`.
       - Public read surface: `public Vector2 Direction { get; private set; }` (per D-04 direct property read).
       - Implement `OnPointerDown(PointerEventData)`, `OnDrag(PointerEventData)`, `OnPointerUp(PointerEventData)`:
         * `OnPointerDown` and `OnDrag` both call private `UpdateDirection(eventData)`.
         * `OnPointerUp` sets `Direction = Vector2.zero` and `handle.anchoredPosition = Vector2.zero`.
       - Implement private `UpdateDirection(PointerEventData eventData)`:
         * Call `RectTransformUtility.ScreenPointToLocalPointInRectangle(background, eventData.position, eventData.pressEventCamera, out Vector2 local)`. NOTE: pass `eventData.pressEventCamera` NOT `null` (Pitfall 4 — Phase 01 canvas is Screen Space - Camera per Plan 01-03 SUMMARY).
         * If the call returns false, early-return.
         * Compute `Vector2 half = background.rect.size * 0.5f;`, then `Vector2 raw = new Vector2(local.x / half.x, local.y / half.y);`.
         * Set `Direction = ApplyDeadZoneAndClamp(raw, deadZone);`.
         * Set `handle.anchoredPosition = new Vector2(Direction.x * half.x, Direction.y * half.y);`.
       - Implement `public static Vector2 ApplyDeadZoneAndClamp(Vector2 raw, float deadZone)` exactly per RESEARCH lines 227-235:
         * `float mag = raw.magnitude;`
         * `if (mag < deadZone) return Vector2.zero;`
         * `if (mag > 1f) return raw / mag;`
         * `float scaled = (mag - deadZone) / (1f - deadZone);`
         * `return (raw / mag) * scaled;`
       - Add `internal void SetDirectionForTesting(Vector2 dir) { Direction = dir; }` for PlayMode test seam (Plan 02-02 consumes it).
    2. Create `Assets/_Project/Scripts/UI/AssemblyInfo.cs` with a single line:
       `[assembly: System.Runtime.CompilerServices.InternalsVisibleTo("WM.Tests.PlayMode")]`
       This grants `SetDirectionForTesting` visibility to the PlayMode test assembly (per RESEARCH §"Plan note" line 844).
    3. Verify `Assets/_Project/Scripts/UI/WM.UI.asmdef` currently has `"references": ["WM.Core", "WM.Economy", "WM.Orders", "WM.Upgrades"]` (verified per RESEARCH line 56 + 162). No reference edits required — `VirtualJoystick` only needs `UnityEngine`, `UnityEngine.UI`, `UnityEngine.EventSystems` which come from the implicit Unity references. Do NOT add `WM.Player` (would create a cycle when Plan 02-02 adds `WM.Player -> WM.UI`).
    4. Verify `Assets/Tests/EditMode/WM.Tests.EditMode.asmdef` references list contains `"WM.UI"` already (per Plan 01-03 SUMMARY line 122). If missing, add it.
    5. EDIT `Assets/Tests/PlayMode/WM.Tests.PlayMode.asmdef`: add `"WM.Player"`, `"WM.UI"`, `"Unity.Cinemachine"` to the `references` array. Final shape:
       ```json
       "references": ["WM.Core", "WM.Player", "WM.UI", "Unity.Cinemachine", "UnityEngine.TestRunner"]
       ```
       Rationale: Plan 02-02 ships `PlayerMovementSmokeTests.cs` and `CameraFollowSmokeTests.cs` that import `WM.Player`, `WM.UI`, and `Unity.Cinemachine`. Adding references now (before those test files exist) prevents an asmdef edit + compile thrash in Plan 02-02.
    6. Create `Assets/Tests/EditMode/VirtualJoystickMathTests.cs` exactly per RESEARCH lines 668-710 (4 test cases: BelowDeadZone, AboveUnitCircle, AtDeadZoneBoundary, HalfwayBetweenDzAndUnit). 2-line header citing `02-VALIDATION.md Wave 0 row 02-01-* + RESEARCH §Pattern 1. Covers MOVE-01 (joystick math)`. `namespace WM.Tests.EditMode`, public unsealed class, `using NUnit.Framework; using UnityEngine; using WM.UI;`.
  </action>
  <acceptance_criteria>
    - [ ] `Assets/_Project/Scripts/UI/VirtualJoystick.cs` exists.
    - [ ] `grep -q 'public Vector2 Direction' Assets/_Project/Scripts/UI/VirtualJoystick.cs` returns 0.
    - [ ] `grep -q 'public static Vector2 ApplyDeadZoneAndClamp' Assets/_Project/Scripts/UI/VirtualJoystick.cs` returns 0.
    - [ ] `grep -q 'eventData.pressEventCamera' Assets/_Project/Scripts/UI/VirtualJoystick.cs` returns 0 (Pitfall 4 mitigation).
    - [ ] `grep -q 'IPointerDownHandler' Assets/_Project/Scripts/UI/VirtualJoystick.cs && grep -q 'IDragHandler' Assets/_Project/Scripts/UI/VirtualJoystick.cs && grep -q 'IPointerUpHandler' Assets/_Project/Scripts/UI/VirtualJoystick.cs` all return 0.
    - [ ] `grep -q 'internal void SetDirectionForTesting' Assets/_Project/Scripts/UI/VirtualJoystick.cs` returns 0.
    - [ ] `grep -q 'InternalsVisibleTo("WM.Tests.PlayMode")' Assets/_Project/Scripts/UI/AssemblyInfo.cs` returns 0.
    - [ ] `grep -q '"WM.Player"' Assets/Tests/PlayMode/WM.Tests.PlayMode.asmdef && grep -q '"WM.UI"' Assets/Tests/PlayMode/WM.Tests.PlayMode.asmdef && grep -q '"Unity.Cinemachine"' Assets/Tests/PlayMode/WM.Tests.PlayMode.asmdef` all return 0.
    - [ ] `Assets/Tests/EditMode/VirtualJoystickMathTests.cs` exists with 4 `[Test]` methods.
    - [ ] EditMode CLI command returns exit 0 and includes all 4 VirtualJoystickMathTests passes:
      ```
      /Applications/Unity/Hub/Editor/6000.4.6f1/Unity.app/Contents/MacOS/Unity -batchmode -projectPath . -runTests -testPlatform EditMode -testFilter WM.Tests.EditMode.VirtualJoystickMathTests -testResults /tmp/wm-vjmath.xml -logFile /tmp/wm-vjmath.log
      ```
      Confirmed via `grep -c 'result="Passed"' /tmp/wm-vjmath.xml` >= 4.
    - [ ] Full EditMode suite still green (57 prior tests + 4 new = 61 minimum):
      ```
      /Applications/Unity/Hub/Editor/6000.4.6f1/Unity.app/Contents/MacOS/Unity -batchmode -projectPath . -runTests -testPlatform EditMode -testResults /tmp/wm-em.xml -logFile /tmp/wm-em.log
      ```
      Exit 0 and `grep 'testcasecount="' /tmp/wm-em.xml` shows >= 61.
  </acceptance_criteria>
  <verify>
    <automated>/Applications/Unity/Hub/Editor/6000.4.6f1/Unity.app/Contents/MacOS/Unity -batchmode -projectPath . -runTests -testPlatform EditMode -testFilter WM.Tests.EditMode.VirtualJoystickMathTests -testResults /tmp/wm-vjmath.xml -logFile /tmp/wm-vjmath.log</automated>
  </verify>
  <done>
    VirtualJoystick.cs compiles cleanly inside WM.UI asmdef, exposes the locked contract (`Vector2 Direction`, `ApplyDeadZoneAndClamp`, `SetDirectionForTesting`), the 4 EditMode math tests pass, and the PlayMode asmdef references are pre-staged for Plan 02-02. No production code consumes the joystick yet — that lands in Plan 02-02.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Author Phase02JoystickBuilder (idempotent Editor builder) and wire joystick into Warehouse_MVP scene</name>
  <read_first>
    - Assets/_Project/Scripts/Editor/Phase01CameraUiBuilder.cs (D-25 sibling-builder template — analog at PATTERNS.md §6; lines 17-25 imports, lines 71-72/190-199 DeleteAllRootGameObjectsNamed, lines 125-130 multi-component GO creation, lines 146-156 RectTransform stretched-full pattern)
    - Assets/_Project/Scripts/Editor/Phase01SceneBuilder.cs (D-22 — for SerializedObject wiring of [SerializeField] private fields at lines 158-166)
    - Assets/_Project/Scripts/Editor/WM.Editor.asmdef (verify already references WM.UI, UnityEngine.UI per Plan 01-03 SUMMARY)
    - Assets/_Project/Scripts/UI/VirtualJoystick.cs (the component to wire — created in Task 1)
    - .planning/phases/02-walk-around-the-warehouse/02-PATTERNS.md §6 (analog file Phase01CameraUiBuilder.cs)
    - .planning/phases/02-walk-around-the-warehouse/02-CONTEXT.md D-01 (bottom-left anchor) + D-02 (always visible) + D-03 (custom UGUI)
  </read_first>
  <files>
    Assets/_Project/Scripts/Editor/Phase02JoystickBuilder.cs (NEW)
    Assets/_Project/Scenes/Warehouse_MVP.unity (MODIFIED — gains Joystick + Handle GameObjects under UICanvas/SafeAreaPanel)
  </files>
  <action>
    1. Create `Assets/_Project/Scripts/Editor/Phase02JoystickBuilder.cs` modelled on `Phase01CameraUiBuilder.cs`:
       - File header per PATTERNS §S2 convention: cite `Plan 02-01 + D-01/D-02/D-03 + RESEARCH §Pattern 1 + PATTERNS §6`, include the headless invocation: `<UNITY> -batchmode -quit -projectPath . -executeMethod WM.Editor.Phase02JoystickBuilder.Build -logFile -`.
       - `using UnityEditor; using UnityEditor.SceneManagement; using UnityEngine; using UnityEngine.SceneManagement; using UnityEngine.UI; using WM.UI;`.
       - `namespace WM.Editor`, `public static class Phase02JoystickBuilder`.
       - `private const string ScenePath = "Assets/_Project/Scenes/Warehouse_MVP.unity";`.
       - `private const float JoystickSize = 280f;` (placeholder — comfortable thumb target on iPhone).
       - `private const float JoystickInset = 200f;` (distance from bottom-left corner of SafeAreaPanel).
       - `[MenuItem("Tools/Phase02/Build Joystick (Plan 02-01)")]` + `public static void Build()`.
    2. In `Build()`:
       - `Scene scene = EditorSceneManager.OpenScene(ScenePath, OpenSceneMode.Single);`
       - Locate `UICanvas` root GameObject: `GameObject uiCanvas = GameObject.Find("UICanvas");`. Throw `InvalidOperationException` with prefix `[Phase02JoystickBuilder]` if null (depends on Plan 01-03).
       - Locate `SafeAreaPanel` child: `Transform safe = uiCanvas.transform.Find("SafeAreaPanel");`. Throw if null.
       - Idempotency: enumerate children of `safe` named `"Joystick"`; `Object.DestroyImmediate` each (analog: Phase01CameraUiBuilder.cs:190-199 + 201-213).
       - Create joystick GameObject: `GameObject joyGo = new GameObject("Joystick", typeof(RectTransform), typeof(Image), typeof(VirtualJoystick));`.
       - `joyGo.transform.SetParent(safe, worldPositionStays: false);`.
       - Configure RectTransform for bottom-left anchor (D-01):
         * `RectTransform rt = joyGo.GetComponent<RectTransform>();`
         * `rt.anchorMin = new Vector2(0f, 0f);`
         * `rt.anchorMax = new Vector2(0f, 0f);`
         * `rt.pivot = new Vector2(0.5f, 0.5f);`
         * `rt.anchoredPosition = new Vector2(JoystickInset, JoystickInset);`
         * `rt.sizeDelta = new Vector2(JoystickSize, JoystickSize);`
         * `rt.localScale = Vector3.one;`
       - Configure background `Image`: `Image bg = joyGo.GetComponent<Image>(); bg.color = new Color(1f, 1f, 1f, 0.35f); bg.raycastTarget = true;`. Leave `sprite = null` (UGUI Image renders a solid color — RESEARCH §"sprite = null" line 379).
       - Create handle child: `GameObject handleGo = new GameObject("Handle", typeof(RectTransform), typeof(Image));`.
       - `handleGo.transform.SetParent(joyGo.transform, worldPositionStays: false);`.
       - Configure handle RectTransform centered: `anchorMin = anchorMax = pivot = new Vector2(0.5f, 0.5f); anchoredPosition = Vector2.zero; sizeDelta = new Vector2(JoystickSize * 0.5f, JoystickSize * 0.5f); localScale = Vector3.one;`.
       - Configure handle `Image`: `Image handleImg = handleGo.GetComponent<Image>(); handleImg.color = Color.white; handleImg.raycastTarget = false;` (handle does not receive pointer events; background does).
       - Wire the `VirtualJoystick` component's `[SerializeField] private` references via SerializedObject (analog: Phase01SceneBuilder.cs:158-166):
         ```csharp
         VirtualJoystick vj = joyGo.GetComponent<VirtualJoystick>();
         SerializedObject so = new SerializedObject(vj);
         SerializedProperty bgProp = so.FindProperty("background");
         SerializedProperty hProp = so.FindProperty("handle");
         if (bgProp == null || hProp == null)
             throw new System.InvalidOperationException("[Phase02JoystickBuilder] SerializedProperty 'background' or 'handle' not found on VirtualJoystick — script must declare both as [SerializeField] private RectTransform.");
         bgProp.objectReferenceValue = rt;
         hProp.objectReferenceValue = handleGo.GetComponent<RectTransform>();
         so.ApplyModifiedPropertiesWithoutUndo();
         ```
       - Save scene:
         ```csharp
         bool saved = EditorSceneManager.SaveScene(scene, ScenePath);
         if (!saved) throw new System.InvalidOperationException($"[Phase02JoystickBuilder] Failed to save scene at {ScenePath}.");
         AssetDatabase.SaveAssets();
         AssetDatabase.Refresh();
         Debug.Log("[Phase02JoystickBuilder] Build complete.");
         ```
    3. Run the builder headlessly to mutate the scene (this is the verification step):
       ```
       /Applications/Unity/Hub/Editor/6000.4.6f1/Unity.app/Contents/MacOS/Unity -batchmode -quit -nographics -projectPath . -executeMethod WM.Editor.Phase02JoystickBuilder.Build -logFile /tmp/wm-jb.log
       ```
       Exit 0 expected; log contains `[Phase02JoystickBuilder] Build complete.`.
  </action>
  <acceptance_criteria>
    - [ ] `Assets/_Project/Scripts/Editor/Phase02JoystickBuilder.cs` exists.
    - [ ] `grep -q 'MenuItem("Tools/Phase02/Build Joystick' Assets/_Project/Scripts/Editor/Phase02JoystickBuilder.cs` returns 0.
    - [ ] `grep -q 'public static void Build()' Assets/_Project/Scripts/Editor/Phase02JoystickBuilder.cs` returns 0.
    - [ ] `grep -q 'DestroyImmediate' Assets/_Project/Scripts/Editor/Phase02JoystickBuilder.cs` returns 0 (idempotency).
    - [ ] `grep -q 'SerializedObject' Assets/_Project/Scripts/Editor/Phase02JoystickBuilder.cs` returns 0 (correct wiring API).
    - [ ] Headless build run produces exit 0 and log contains `[Phase02JoystickBuilder] Build complete.`.
    - [ ] Post-build scene grep proves the joystick is wired:
      * `grep -c "m_Name: Joystick$" Assets/_Project/Scenes/Warehouse_MVP.unity` returns >= 1.
      * `grep -c "m_Name: Handle$" Assets/_Project/Scenes/Warehouse_MVP.unity` returns >= 1.
      * `grep -q 'VirtualJoystick' Assets/_Project/Scenes/Warehouse_MVP.unity` returns 0 (component MonoScript reference present).
    - [ ] Re-running the builder a second time succeeds (exit 0) AND the count of `m_Name: Joystick$` in the scene remains exactly 1 (idempotency):
      ```
      /Applications/Unity/Hub/Editor/6000.4.6f1/Unity.app/Contents/MacOS/Unity -batchmode -quit -nographics -projectPath . -executeMethod WM.Editor.Phase02JoystickBuilder.Build -logFile /tmp/wm-jb-2.log && [ "$(grep -c 'm_Name: Joystick$' Assets/_Project/Scenes/Warehouse_MVP.unity)" = "1" ]
      ```
    - [ ] EditMode + PlayMode test suites stay green (run for sampling-rate compliance):
      ```
      /Applications/Unity/Hub/Editor/6000.4.6f1/Unity.app/Contents/MacOS/Unity -batchmode -projectPath . -runTests -testPlatform EditMode -testResults /tmp/wm-em-t2.xml -logFile /tmp/wm-em-t2.log
      /Applications/Unity/Hub/Editor/6000.4.6f1/Unity.app/Contents/MacOS/Unity -batchmode -projectPath . -runTests -testPlatform PlayMode -testResults /tmp/wm-pm-t2.xml -logFile /tmp/wm-pm-t2.log
      ```
      Both exit 0. No new PlayMode tests yet — existing `PlayModeSmokeTests.Scene_Loads_GameManagerInitializes` must remain green (joystick GameObject must not break Bootstrap chain).
  </acceptance_criteria>
  <verify>
    <automated>/Applications/Unity/Hub/Editor/6000.4.6f1/Unity.app/Contents/MacOS/Unity -batchmode -quit -nographics -projectPath . -executeMethod WM.Editor.Phase02JoystickBuilder.Build -logFile /tmp/wm-jb.log && grep -c "m_Name: Joystick$" Assets/_Project/Scenes/Warehouse_MVP.unity</automated>
  </verify>
  <done>
    Joystick GameObject is present in `Warehouse_MVP.unity` under `UICanvas/SafeAreaPanel`, the `VirtualJoystick` component's `background` and `handle` references are wired via SerializedObject (no Inspector clicks required), the builder is idempotent (second run leaves scene byte-stable), and the full test suite remains green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Touch screen -> EventSystem -> UGUI handler | Local hardware input. No network, no IPC, no untrusted data crosses any process boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Spoofing | VirtualJoystick / EventSystem | N/A | No external attack surface in this phase. Player input is local touch screen -> in-process method calls. No threats applicable at ASVS L1. |
| T-02-02 | Tampering | VirtualJoystick.SetDirectionForTesting | N/A | Method is `internal`, exposed only to `WM.Tests.PlayMode` via `InternalsVisibleTo`. Not reachable from production code or external assemblies. |
| T-02-03 | Repudiation | — | N/A | No persistence, no audit trail, no user identity in this phase. |
| T-02-04 | Information Disclosure | — | N/A | No PII, no secrets, no network. |
| T-02-05 | Denial of Service | — | N/A | UGUI handler dispatch is bounded by Unity's frame loop; no unbounded loops, no recursion. |
| T-02-06 | Elevation of Privilege | — | N/A | Process boundary unchanged; no sandbox escapes. |
</threat_model>

<verification>
**Per-plan gate:**
- EditMode suite green: 61+ passing tests (57 prior + 4 new).
- PlayMode suite green: existing 1/1 still passes (`Scene_Loads_GameManagerInitializes`).
- `Phase02JoystickBuilder.Build` is idempotent (verified by re-run + grep count).
- `Warehouse_MVP.unity` gains exactly one `Joystick` and one `Handle` named GameObject.

**Manual verification deferred to phase-level orchestrator** (per 02-VALIDATION.md "Joystick feel on physical iPhone" row — Plan 02-01 surface is automated-only).
</verification>

<success_criteria>
1. `Assets/_Project/Scripts/UI/VirtualJoystick.cs` implements the locked `Vector2 Direction` contract with dead-zone math and pointer handlers (D-03, D-04 satisfied).
2. The joystick widget is present in the Warehouse_MVP scene under `UICanvas/SafeAreaPanel`, anchored bottom-left (D-01, D-02 satisfied).
3. Wave 0 `VirtualJoystickMathTests` covers all four pure-function cases and passes via CLI.
4. Test asmdef references are pre-staged for Plan 02-02 PlayMode tests to compile on first run.
5. The plan does NOT touch `WM.Player` or `PlayerController` — those land in Plan 02-02.
</success_criteria>

<output>
After completion, create `.planning/phases/02-walk-around-the-warehouse/02-01-SUMMARY.md` per the standard SUMMARY template. Include:
- `Threat Model Verification` section (all T-02-XX listed as N/A per security_threat_model_requirement).
- `Expected drift` note for `ProjectSettings/ProjectSettings.asset` `iOSSupport.m_Automatic` flip (Pitfall 6).
- Handoff note for Plan 02-02: "VirtualJoystick contract locked. PlayerController in WM.Player will consume `joystick.Direction` per D-04 direct property read. PlayMode asmdef already references WM.Player + WM.UI + Unity.Cinemachine."
</output>
</content>
</invoke>